cp -r hypr "$HOME/.config/" && echo "hypr copied into $HOME/.config/"
cp -r fontconfig "$HOME/.config/" && echo "fontconfig copied into $HOME/.config/"
cp -r kitty "$HOME/.config/" && echo "kitty copied into $HOME/.config/"
cp -r neofetch "$HOME/.config/" && echo "neofetch copied into $HOME/.config/"
cp -r waybar "$HOME/.config/" && echo "waybar copied into $HOME/.config/"
cp -r wofi "$HOME/.config/" && echo "wofi copied into $HOME/.config/"
cp -r NetworkManager/ "/etc/NetworkManager/conf.d/"&& echo "copied networkmanager random mac" 




